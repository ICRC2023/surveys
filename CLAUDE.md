# CLAUDE.md

This file provides Claude Code (claude.ai/code) specific guidance for this repository.

**All shared guidance (project overview, architecture, workflows, code style, security, and common gotchas) lives in [`AGENTS.md`](AGENTS.md). Read it first.** This file only records notes specific to Claude Code.

## Claude Code specific notes

- Use markdown link syntax for file references (e.g. `[cli.py](titanite/cli.py)`), not backticks or HTML tags.
- Respond in Japanese, per the conventions in `AGENTS.md`.
- Prefer the dedicated file/search tools over shell equivalents (`cat`, `sed`, `grep`, etc.).
- Follow the git worktree and issue-driven workflows documented in `AGENTS.md` for all branch development.
